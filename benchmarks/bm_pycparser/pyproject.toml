[project]
name = "bm_pycparser"
dependencies = [
    "pycparser",
]

# XXX This should be inherited from metabase.
dynamic = [
  "name",
  "version",
]

[tool.pyperformance]
# "name" is set automatically.
metabase = ".."
